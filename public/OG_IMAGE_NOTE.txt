Current social-share image: `/rsp-share-preview.jpg`.
This file is referenced by the site metadata in `app/layout.tsx` and `app/page.tsx`.
If you replace it later, keep the image share-friendly and update the metadata path if the filename changes.
